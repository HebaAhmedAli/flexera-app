android {
    packagingOptions {
        // Exclude all Crashlytics native libraries using wildcard
        exclude 'lib/**/libcrashlytics*.so'

        jniLibs {
            useLegacyPackaging = false
        }
    }
}
